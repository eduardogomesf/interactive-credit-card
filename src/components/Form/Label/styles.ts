import styled from "styled-components";

export const Container = styled.span`
    display: block;
    margin-bottom: 9px;

    font-weight: 500;
    font-size: 12px;
    line-height: 15px;
    letter-spacing: 2px;
    text-transform: uppercase;

    color: #21092F;
`